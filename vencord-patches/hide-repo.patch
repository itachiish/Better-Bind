diff --git a/src/components/settings/tabs/updater/Components.tsx b/src/components/settings/tabs/updater/Components.tsx
index 67c3b44..137e6ab 100644
--- a/src/components/settings/tabs/updater/Components.tsx
+++ b/src/components/settings/tabs/updater/Components.tsx
@@ -7,7 +7,6 @@
 import { Card } from "@components/Card";
 import { ErrorCard } from "@components/ErrorCard";
 import { Flex } from "@components/Flex";
-import { Link } from "@components/Link";
 import { Margins } from "@utils/margins";
 import { classes } from "@utils/misc";
 import { relaunch } from "@utils/native";
@@ -21,12 +20,9 @@ export interface CommonProps {
     repoPending: boolean;
 }
 
-export function HashLink({ repo, hash, disabled = false }: { repo: string, hash: string, disabled?: boolean; }) {
-    return (
-        <Link href={`${repo}/commit/${hash}`} disabled={disabled}>
-            {hash}
-        </Link>
-    );
+// Modifié : on n'expose pas le dépôt (pas de lien vers GitHub), juste le hash en texte.
+export function HashLink({ hash }: { repo?: string; hash: string; disabled?: boolean; }) {
+    return <span>{hash}</span>;
 }
 
 export function Changes({ updates, repo, repoPending }: CommonProps & { updates: typeof changes; }) {
diff --git a/src/components/settings/tabs/updater/index.tsx b/src/components/settings/tabs/updater/index.tsx
index 30191cb..9101000 100644
--- a/src/components/settings/tabs/updater/index.tsx
+++ b/src/components/settings/tabs/updater/index.tsx
@@ -32,9 +32,7 @@ import { useAwaiter } from "@utils/react";
 import { getRepo, isNewer, UpdateLogger } from "@utils/updater";
 import { Forms, React } from "@webpack/common";
 
-import gitHash from "~git-hash";
-
-import { CommonProps, HashLink, Newer, Updatable } from "./Components";
+import { CommonProps, Newer, Updatable } from "./Components";
 
 function VesktopSection() {
     if (!IS_VESKTOP) return null;
@@ -95,23 +93,6 @@ function Updater() {
                 disabled={!settings.autoUpdate}
             />
 
-            <Forms.FormTitle tag="h5" className={Margins.top20}>Repo</Forms.FormTitle>
-
-            <Forms.FormText>
-                {repoPending
-                    ? repo
-                    : err
-                        ? "Failed to retrieve - check console"
-                        : (
-                            <Link href={repo}>
-                                {repo.split("/").slice(-2).join("/")}
-                            </Link>
-                        )
-                }
-                {" "}
-                (<HashLink hash={gitHash} repo={repo} disabled={repoPending} />)
-            </Forms.FormText>
-
             <Divider className={classes(Margins.top16, Margins.bottom16)} />
 
             <Forms.FormTitle tag="h5">Updates</Forms.FormTitle>
